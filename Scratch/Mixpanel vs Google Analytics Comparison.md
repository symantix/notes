The primary difference that I'm seeing between the Mixpanel and GA implementations, is that Mixpanel has an explicit call within the application components for tracking, and is explicitly set to track every step of the quiz.  By contrast, the GA implementation appears to be executed by an installed module with no specific call within the application.  This may yield a less customized, less controlled set of results in GA.  On the other hand, the GA module is likely executing a tracking command on every page, which would likely allow for a funnel to be organized within GA's own admin.  However, since this application is in SPA mode, GA may not be tracking every actual page or step.

If needed, we can discuss the differences I’m seeing in our standup tomorrow.  But if we are going to switch our application to universal mode, that may resolve any differences in the result data all by itself (possibly).