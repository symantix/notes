Had some trouble initiating the Netlify function and detecting the user IP, but that's sorted now.  I'm also filtering out any mix panel tracking from non-live domains.

We can't filter these results retro-actively.  Those results will have to be removed manually from Mixpanel, if possible.